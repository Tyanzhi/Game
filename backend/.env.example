DATABASE_URL=postgresql+asyncpg://world_engine:change_me@localhost:5432/world_engine
REDIS_URL=redis://localhost:6379/0

APP_ROLE=api
AUTO_CREATE_SCHEMA=1
EMBEDDED_LIVE_SCHEDULER=1

CORS_ALLOWED_ORIGINS=http://localhost:5173,https://geopolitica20261.netlify.app
LIVE_INTELLIGENCE_INTERVAL_SECONDS=300
LIVE_SIMULATION_ID=live-world

AI_PROVIDER_KEY=
AI_MODEL=


# Distributed live-worker leader election
LIVE_WORKER_LEASE_SECONDS=900
LIVE_WORKER_STANDBY_POLL_SECONDS=30

# Fly.io production keeps the API scheduler disabled; the worker is a separate process group.
# DATABASE_URL and REDIS_URL must be configured as Fly secrets, not committed.
